@model IEnumerable<AutoCar360.Controllers.PROXIMOMANTENIMIENTO>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProximoMantenimiento_Km)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MANTENIMIENTOVEHICULO.Id_MantenimientoVehiculo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ProximoMantenimiento_Km)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MANTENIMIENTOVEHICULO.Id_MantenimientoVehiculo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_ProximoMantenimiento }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_ProximoMantenimiento }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_ProximoMantenimiento })
        </td>
    </tr>
}

</table>
