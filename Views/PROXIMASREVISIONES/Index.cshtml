@model IEnumerable<AutoCar360.Controllers.PROXIMASREVISIONES>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a href="@Url.Action("Create", "REVISIONESVEHICULOes")" class="">Realizar mantenimiento</a>
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProximaRevision_intervaloKm)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProximaRevision_intervaloTiempo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.REVISIONESVEHICULO.Id_RevisionesVehiculo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ProximaRevision_intervaloKm)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProximaRevision_intervaloTiempo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.REVISIONESVEHICULO.Id_RevisionesVehiculo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_ProximaRevision }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_ProximaRevision }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_ProximaRevision })
        </td>
    </tr>
}

</table>
