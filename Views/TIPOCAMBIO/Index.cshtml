@model IEnumerable<AutoCar360.Controllers.TIPOCAMBIO>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TipoCambio_Nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TipoCambio_Nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_TipoCambio }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_TipoCambio }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_TipoCambio })
        </td>
    </tr>
}

</table>
