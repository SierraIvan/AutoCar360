@model IEnumerable<AutoCar360.Controllers.MANTENIMIENTOVEHICULO>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MantenimientoVehiculo_IntervaloKm)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TIPOMANTENIMIENTO.Mantenimiento_Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VEHICULOS.Vehiculo_Matricula)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MantenimientoVehiculo_IntervaloKm)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TIPOMANTENIMIENTO.Mantenimiento_Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VEHICULOS.Vehiculo_Matricula)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_MantenimientoVehiculo }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_MantenimientoVehiculo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_MantenimientoVehiculo })
        </td>
    </tr>
}

</table>
